# O Metro (bundler do React Native/Expo) não lida bem com o
# node_modules "simbólico" padrão do pnpm em monorepos. Usar
# node-linker=hoisted faz o pnpm montar um node_modules mais
# parecido com o do npm/yarn clássico, que o Metro entende.
node-linker=hoisted
